{
	"type": "module",
	"scripts": {
		"build": "vite build --app && PRERENDER=1 vite build --app",
		"preview": "vite preview",
		"dev": "vite"
	},
	"dependencies": {
		"preact": "^10.25.4",
		"preact-iso": "^2.8.1"
	},
	"devDependencies": {
		"@preact/preset-vite": "^2.10.0",
		"vite": "^6.0.11"
	}
}
